package domain.objectDetection;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * First Implementation of Fault Object Detection
 * 
 * @author Michael C. Kunkel
 *
 */
public class FaultObjectDetection {
	private static final Logger log = LoggerFactory.getLogger(FaultObjectDetection.class);

	public static void main(String[] args) {
		// parameters matching the Fault Data O model
		int width = 6;
		int height = 112;
		int nChannels = 1;
		int gridWidth = 1;
		int gridHeight = 3;
	}
}
